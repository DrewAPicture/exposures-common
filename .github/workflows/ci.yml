name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Android build
        uses: ./.github/actions/setup-android-build

      - name: Run tests
        run: ./gradlew test
